---
// Testimonials data
const testimonials = [
  {
    name: "PRIYA S.",
    location: "Mumbai, India",
    avatar: "⭐",
    avatarBg: "#FF4D4D",
    quote: "I was stuck trading hours for clients as a therapist. Now, my 'Anxiety Relief Guide' sells while I sleep. This changed everything.",
    tags: [
      { text: "Digital Product Creator", color: "#FF4D4D" },
      { text: "Works While You Nap", color: "#2ECC71" }
    ]
  },
  {
    name: "OLUWAUEUM A.",
    location: "Lagos, Nigeria",
    avatar: "👨‍🍳",
    avatarBg: "#4A90E2",
    quote: "As a chef, I thought online business was too complicated. My 'West African Recipes' PDF made its first sales within weeks. Now I help others do the same!",
    tags: [
      { text: "Digital Product Creator", color: "#4A90E2" },
      { text: "Global Reach", color: "#2ECC71" }
    ]
  },
  {
    name: "SOPHIE M.",
    location: "Paris, France",
    avatar: "👗",
    avatarBg: "#FFD700",
    quote: "My fashion blog had followers but no income. After launching my 'Capsule Wardrobe PDF,' I replaced my Paris retail job. Merci!",
    tags: [
      { text: "Digital Product Creator", color: "#FFD700" },
      { text: "Location Freedom", color: "#2ECC71" }
    ]
  }
];
---

<section id="success-stories" class="relative py-24 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 bg-gradient-to-br from-yellow-100 via-yellow-50 to-white"></div>
    <div class="absolute inset-0 bg-halftone opacity-5 pointer-events-none mix-blend-multiply animate-subtle-scale"></div>
    
    <!-- Enhanced Decorative Elements -->
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-10 left-10 text-6xl animate-sparkle">✨</div>
        <div class="absolute bottom-20 right-10 text-6xl animate-sparkle" style="animation-delay: 1s">⭐</div>
        <div class="absolute top-1/3 right-1/4 text-5xl animate-sparkle" style="animation-delay: 0.5s">🌟</div>
        <div class="absolute bottom-1/3 left-1/4 text-5xl animate-sparkle" style="animation-delay: 1.5s">💫</div>
    </div>

    <div class="container mx-auto px-4 relative z-10">
        <!-- Enhanced Header -->
        <div class="text-center mb-16 reveal-on-scroll">
            <div class="comic-bubble bg-white inline-block transform -rotate-2 mb-6 hover:rotate-2 transition-all duration-300">
                <p class="text-xl font-bold relative inline-block">
                    <span class="relative z-10">CUSTOMER EXPERIENCES</span>
                    <span class="absolute inset-0 bg-yellow-200 -rotate-3 z-0"></span>
                </p>
            </div>
            
            <h2 class="comic-title text-5xl md:text-6xl mb-8 max-w-4xl mx-auto transform hover:scale-105 transition-all duration-300">
                <span class="relative inline-block">
                    REAL PEOPLE, REAL FREEDOM
                    <div class="absolute bottom-0 left-0 right-0 h-4 bg-[#FFE066] -rotate-1 -z-10 animate-width"></div>
                </span>
            </h2>
            
            <p class="text-lg text-gray-700 mb-4">No big audience required. Just a simple method that works.</p>
        </div>

        <!-- Enhanced Testimonial Cards -->
        <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-16">
            {testimonials.map((testimonial, index) => (
                <div 
                    class="testimonial-card comic-card bg-white transform hover:scale-105 hover:-rotate-2 transition-all duration-300"
                    style={`animation-delay: ${index * 200}ms`}
                >
                    <!-- Enhanced Avatar & Info -->
                    <div class="flex items-center gap-4 p-6 border-b-4 border-black relative overflow-hidden">
                        <div class="avatar-wrapper relative">
                            <div 
                                class="w-16 h-16 rounded-full border-4 border-black flex items-center justify-center text-2xl transform hover:rotate-12 transition-all duration-300"
                                style={`background: ${testimonial.avatarBg}`}
                            >
                                {testimonial.avatar}
                            </div>
                            <div class="avatar-glow absolute inset-0 rounded-full"></div>
                        </div>
                        <div class="info-wrapper">
                            <h3 class="comic-title text-xl hover:scale-105 transition-transform duration-300">{testimonial.name}</h3>
                            <p class="text-gray-600 location-text">{testimonial.location}</p>
                        </div>
                        <div class="shine-overlay"></div>
                    </div>
                    
                    <!-- Enhanced Quote -->
                    <div class="p-6 quote-wrapper">
                        <p class="text-lg mb-6 font-comic quote-text">"{testimonial.quote}"</p>
                        
                        <!-- Enhanced Tags -->
                        <div class="flex flex-wrap gap-2">
                            {testimonial.tags.map((tag) => (
                                <span 
                                    class="tag-pill inline-flex items-center px-3 py-1 rounded-full text-sm font-bold border-2 border-black transform hover:scale-110 hover:rotate-3 transition-all duration-300"
                                    style={`background: ${tag.color}; color: white;`}
                                >
                                    {tag.text}
                                </span>
                            ))}
                        </div>
                    </div>
                </div>
            ))}
        </div>

        <!-- Enhanced CTA -->
        <div class="text-center reveal-on-scroll">
            <div class="comic-bubble bg-white text-black inline-block transform hover:rotate-3 transition-all duration-300 mb-8 animate-bubble-pulse">
                <p class="text-2xl font-bold flex items-center gap-2">
                    Still unsure? That's okay. 
                    <span class="animate-bounce-slow inline-block">💤</span>
                </p>
                <div class="absolute -bottom-4 left-1/2 transform -translate-x-1/2 w-0 h-0 border-l-8 border-r-8 border-t-8 border-transparent border-t-white"></div>
            </div>
            
            <p class="text-lg text-gray-700 mb-4">You get 30 days to try it. No pressure, no follow-ups. This is self-paced, no-support-required. You get the files, the step-by-step guide, and the freedom—without any back-and-forth.</p>
            
            <a 
                href="#get-started" 
                class="cta-button comic-button bg-black text-white hover:bg-comic-accent transform hover:scale-105 hover:-rotate-2 transition-all duration-300 inline-flex items-center gap-2 animate-bounce-subtle"
            >
                <p class="text-2xl font-bold text-white mb-4">YOU DO NOT NEED A HUGE AUDIENCE. YOU JUST NEED A SIMPLE METHOD THAT WORKS.</p>
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 animate-arrow-bounce" viewBox="0 0 20 20" fill="currentColor">
                    <path fill-rule="evenodd" d="M10.293 3.293a1 1 0 011.414 0l6 6a1 1 0 010 1.414l-6 6a1 1 0 01-1.414-1.414L14.586 11H3a1 1 0 110-2h11.586l-4.293-4.293a1 1 0 010-1.414z" clip-rule="evenodd" />
                </svg>
            </a>
        </div>
    </div>
</section>

<style>
    .comic-card {
        border: 4px solid black;
        border-radius: 16px;
        box-shadow: 8px 8px 0 rgba(0, 0, 0, 0.2);
        overflow: hidden;
    }

    .comic-card:hover {
        box-shadow: 12px 12px 0 rgba(0, 0, 0, 0.15);
    }

    .comic-bubble {
        border: 4px solid black;
        border-radius: 12px;
        padding: 1rem 1.5rem;
        box-shadow: 4px 4px 0 rgba(0, 0, 0, 0.2);
    }

    .bg-halftone {
        background-image: radial-gradient(circle at center, black 1px, transparent 1px);
        background-size: 24px 24px;
        background-position: center;
    }

    @keyframes bounce-subtle {
        0%, 100% { transform: translateY(0); }
        50% { transform: translateY(-4px); }
    }

    .animate-bounce-subtle {
        animation: bounce-subtle 2s ease-in-out infinite;
    }

    /* Scroll animation */
    .comic-card {
        opacity: 0;
        transform: translateY(20px);
        transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .comic-card.visible {
        opacity: 1;
        transform: translateY(0);
    }

    /* New Animations */
    .animate-subtle-scale {
        animation: subtle-scale 15s ease-in-out infinite;
    }

    .animate-sparkle {
        animation: sparkle 3s ease-in-out infinite;
        filter: drop-shadow(0 0 10px rgba(255, 215, 0, 0.5));
    }

    @keyframes subtle-scale {
        0%, 100% {
            transform: scale(1) rotate(0deg);
            opacity: 0.05;
        }
        50% {
            transform: scale(1.1) rotate(1deg);
            opacity: 0.07;
        }
    }

    @keyframes sparkle {
        0%, 100% {
            transform: scale(1) rotate(0deg);
            opacity: 0.8;
        }
        50% {
            transform: scale(1.2) rotate(15deg);
            opacity: 1;
        }
    }

    @keyframes width {
        0% {
            width: 0;
        }
        100% {
            width: 100%;
        }
    }

    .animate-width {
        animation: width 1.5s ease-out forwards;
    }

    @keyframes bubble-pulse {
        0%, 100% {
            transform: scale(1) rotate(0deg);
        }
        50% {
            transform: scale(1.05) rotate(2deg);
        }
    }

    .animate-bubble-pulse {
        animation: bubble-pulse 3s ease-in-out infinite;
    }

    @keyframes bounce-slow {
        0%, 100% {
            transform: translateY(0);
        }
        50% {
            transform: translateY(-8px);
        }
    }

    .animate-bounce-slow {
        animation: bounce-slow 2s ease-in-out infinite;
    }

    @keyframes arrow-bounce {
        0%, 100% {
            transform: translateX(0);
        }
        50% {
            transform: translateX(5px);
        }
    }

    .animate-arrow-bounce {
        animation: arrow-bounce 1.5s ease-in-out infinite;
    }

    .stat-card {
        position: relative;
        overflow: hidden;
    }

    .stat-icon-wrapper {
        position: relative;
        display: inline-block;
    }

    .stat-icon {
        position: relative;
        z-index: 2;
    }

    .stat-icon-wrapper::after {
        content: '';
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 60px;
        height: 60px;
        background: rgba(255, 255, 255, 0.8);
        border-radius: 50%;
        z-index: 1;
        filter: blur(5px);
    }

    .number-wrapper {
        position: relative;
        overflow: hidden;
    }

    .animate-number {
        position: relative;
        display: inline-block;
    }

    .animate-number::after {
        content: '';
        position: absolute;
        bottom: 0;
        left: 0;
        width: 100%;
        height: 2px;
        background: currentColor;
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s ease-out;
    }

    .stat-card:hover .animate-number::after {
        transform: scaleX(1);
    }

    .testimonial-card {
        position: relative;
        overflow: hidden;
    }

    .avatar-wrapper {
        position: relative;
    }

    .avatar-glow {
        background: radial-gradient(circle at center, rgba(255, 255, 255, 0.8) 0%, transparent 70%);
        filter: blur(5px);
        opacity: 0;
        transition: opacity 0.3s ease-out;
    }

    .avatar-wrapper:hover .avatar-glow {
        opacity: 1;
    }

    .shine-overlay {
        position: absolute;
        top: 0;
        left: -100%;
        width: 50%;
        height: 100%;
        background: linear-gradient(to right, transparent, rgba(255, 255, 255, 0.3), transparent);
        transform: skewX(-25deg);
        transition: left 0.5s ease-out;
    }

    .testimonial-card:hover .shine-overlay {
        left: 150%;
    }

    .quote-wrapper {
        position: relative;
    }

    .quote-text {
        position: relative;
        padding-left: 1rem;
        border-left: 3px solid #FFE066;
    }

    .tag-pill {
        position: relative;
        overflow: hidden;
    }

    .tag-pill::after {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(255, 255, 255, 0.2);
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s ease-out;
    }

    .tag-pill:hover::after {
        transform: scaleX(1);
    }

    .location-text {
        position: relative;
        display: inline-block;
    }

    .location-text::after {
        content: '';
        position: absolute;
        bottom: -2px;
        left: 0;
        width: 100%;
        height: 1px;
        background: currentColor;
        transform: scaleX(0);
        transform-origin: left;
        transition: transform 0.3s ease-out;
    }

    .info-wrapper:hover .location-text::after {
        transform: scaleX(1);
    }

    .reveal-on-scroll {
        opacity: 0;
        transform: translateY(20px);
        transition: all 0.6s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .reveal-on-scroll.visible {
        opacity: 1;
        transform: translateY(0);
    }
</style>

<script>
    // Intersection Observer for scroll animations
    document.addEventListener('DOMContentLoaded', () => {
        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('visible');
                }
            });
        }, {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        });

        // Observe all cards and headers
        document.querySelectorAll('.comic-card, .reveal-on-scroll').forEach(el => {
            observer.observe(el);
        });
    });
</script> 